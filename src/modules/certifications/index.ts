/**
 * Certifications Module — barrel export
 *
 * Generates and verifies SHA-256-based document authenticity certificates.
 * Business logic will be implemented in the next prompt.
 */

export type { CertificationMeta } from '@/types'
