/**
 * Access Requests Module — barrel export
 *
 * Request and approval workflow for accessing restricted documents.
 * Business logic will be implemented in the next prompt.
 */

export type { AccessRequestStatus } from '@/types'
