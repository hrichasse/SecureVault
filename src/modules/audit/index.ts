/**
 * Audit Module — barrel export
 *
 * Full audit trail: logs every action taken on documents and users.
 * Business logic will be implemented in the next prompt.
 */

export type { AuditAction, AuditEntry } from '@/types'
