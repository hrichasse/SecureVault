/**
 * Incidents Module — barrel export
 *
 * Security incident reporting, management, and resolution.
 * Business logic will be implemented in the next prompt.
 */

export type { IncidentSeverity, IncidentStatus } from '@/types'
